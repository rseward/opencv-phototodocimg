# Purpose

An opencv example project for extracting an image of a document from a photo. Currently a simple project for finding a single page in an image and extracting it. The page image could be processed further with OCR or other image processing techniques.

# Usage

```bash
uv run phototodocimg.py --image <image_path> --debug
```

